a = 1
b = 2
c = 3
d = 4

if a >= b:
    if a >= c:
        if a >= d:
            print(f"a가 최대값")

if b >= a:
    if b >= c:
        if b >= d:
            print(f"b가 최대값")

if c >= a:
    if c >= b:
        if c >= d:
            print(f"c가 최대값")

if d >= a:
    if d >= b:
        if d >= c:
            print(f"d가 최대값")
